#include <iostream>
using namespace std;

int main()
{
    /*
        1. Find the subarray in an array which has maximum sum.

    */
    // int n;
    // cout << "Enter array size\n";
    // cin >> n;

    // int arr[n];
    // cout << "Enter array element\n";
    // for(int i=0; i<n; ++i) {
    //     cin >> arr[i];
    // }

    // int cumArr[n], sum = 0;
    // for(int i=0; i<n; ++i) {
    //     sum += arr[i];
    //     cumArr[i] = sum;
    // }

    // for(int i=0; i<n; ++i) {
    //     cout << cumArr[i] << " ";
    // }

    return 0;
}